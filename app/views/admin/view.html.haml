= javascript_include_tag 'admin'

#title.page_title
  %h1 TTA's PROJECT INFORMATION
- if @json
  = render partial: '/admin/admin_partial'

:javascript
  $(document).ready(function () {
      ProductStatistics.renderTable("#{@json}");
  });

  $(document).ready(function () {
      ProductStatistics.renderTable("#{@json}");
  });
