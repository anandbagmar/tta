<%= render 'layouts/compare_partial' %>

<script type="text/javascript">
    $(document).ready(function () {
        var jsonDataSet = "<%= @compare_json %>";
        jsonDataSet = jsonData.parse(jsonDataSet);
        if (jsonDataSet != "") {
            compareRuns.renderView(jsonDataSet);
        }
    });
</script>